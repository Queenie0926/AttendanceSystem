// Copy this file to "arduino_secrets.h" and fill in the real values.
// arduino_secrets.h is gitignored and must never be committed.
//
// In the Arduino IDE, arduino_secrets.h must sit in the same folder as
// attendance_device.ino - it will show up as a second tab in the editor.

#pragma once

#define SECRET_WIFI_SSID     "your-wifi-name"
#define SECRET_WIFI_PASSWORD "your-wifi-password"

#define SECRET_SUPABASE_REF  "your-project-ref"
#define SECRET_DEVICE_KEY    "your-device-api-key"
